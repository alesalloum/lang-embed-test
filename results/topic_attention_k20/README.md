# Topic attention artifacts (K=20 microtopics)

Generated by `scripts/cluster_user_attention.py --n-clusters 20` for the NMF
latent-profile rank-selection experiment.

Soft user attention (`user_attention_soft.*`) is the matrix **X**
(N_users × 20) used by `scripts/nmf_latent_profiles.py`.

See [`../nmf_latent_profiles/`](../nmf_latent_profiles/) for NMF elbow results.

`post_assignments.*` are omitted from git (large); regenerate with the command above
if needed.
